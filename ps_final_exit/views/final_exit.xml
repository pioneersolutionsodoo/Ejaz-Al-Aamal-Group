<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>

        <record model="ir.ui.view" id="final_exit_tree_view">
            <field name="name">Final Exit List View</field>
            <field name="model">final.exit</field>
            <field name="arch" type="xml">
                <tree>
                    <field name="name"/>
                    <field name="employee_code" optional="hide"/>
                    <field name="employee_id"/>
                    <field name="department_id" optional="hide"/>
                    <field name="job_id" optional="show"/>
                    <field name="identification_id" optional="hide"/>
                    <field name="passport_id" optional="hide"/>
                    <field name="issue_date"/>
                    <field name="depart_from"/>
                    <field name="arrive_at"/>
                    <field name="travel_date"/>
                    <field name="recommended_time" optional="show"/>
                    <field name="state" widget="badge" decoration-info="state in ('draft', 'wait_4_hr_mgr')"
                           decoration-success="state == 'approve'" decoration-danger="state == 'refuse'" />
                </tree>
            </field>
        </record>


        <record model="ir.ui.view" id="final_exit_form_view">
            <field name="name">Final Exit Form View</field>
            <field name="model">final.exit</field>
            <field name="arch" type="xml">
                <form>
                    <header>
                        <button name="action_send_request" type="object" states="draft" string="Request From HRM" class="oe_highlight"/>
                        <button name="action_approve" type="object" states="wait_4_hr_mgr" string="Approve" class="oe_highlight"
                                groups="hr.group_hr_manager"/>
                        <button name="action_refuse" type="object" states="wait_4_hr_mgr" string="Reject"
                                groups="hr.group_hr_manager"/>
                        <button name="action_cancel" type="object" states="wait_4_hr_mgr" string="Cancel"
                                groups="hr.group_hr_manager"/>
                        <button name="action_draft" type="object" states="cancel" string="Set to Draft"
                                groups="hr.group_hr_manager"/>
                        <field name="state" widget="statusbar" statusbar_visible="draft,approve"/>
                    </header>

                    <sheet>
                        <div class="oe_title">
                            <h1>
                                <field name="name" readonly="1"/>
                            </h1>
                        </div>
                        <group>
                            <group>
                                <field name="employee_id" options="{'no_create': True}" attrs="{'readonly':[('state', 'not in', ('draft'))]}"/>
                                <field name="employee_code"/>
                                <field name="job_id"/>
                                <field name="department_id"/>
                                <field name="country_id"/>
                                <field name="birthday"/>
                                <field name="work_phone"/>
                            </group>
                            <group >
                                <field name="identification_id"/>
                                <field name="passport_id"/>
                                <field name="issue_date" attrs="{'readonly':[('state', 'not in', ('draft'))]}"/>
                                <field name="depart_from" attrs="{'readonly':[('state', 'not in', ('draft'))]}"/>
                                <field name="arrive_at" attrs="{'readonly':[('state', 'not in', ('draft'))]}"/>
                                <field name="travel_date" attrs="{'readonly':[('state', 'not in', ('draft'))]}"/>
                                <field name="recommended_time" attrs="{'readonly':[('state', 'not in', ('draft'))]}"/>
                                <field name="grade" attrs="{'readonly':[('state', 'not in', ('draft'))]}"/>
                            </group>
                        </group>

                        <notebook>
                            <page string="Notes">
                                <group>
                                    <field name="note" nolabel="1" attrs="{'readonly':[('state', 'not in', ('draft','wait_4_hr_mgr'))]}"/>
                                </group>
                            </page>
                        </notebook>
                    </sheet>
                    <div class="oe_chatter">
                        <field name="message_follower_ids"/>
                        <field name="activity_ids"/>
                        <field name="message_ids" options="{'post_refresh': 'recipients'}"/>
                    </div>
                </form>
            </field>
        </record>



        <record model="ir.ui.view" id="final_exit_search_view">
            <field name="name">Final Exit Search View</field>
            <field name="model">final.exit</field>
            <field name="arch" type="xml">
                <search>
                    <field name="name"/>
                    <field name="employee_code"/>
                    <field name="employee_id"/>
                    <field name="department_id"/>
                    <field name="job_id"/>
                    <field name="work_phone"/>
                    <field name="identification_id"/>
                    <field name="passport_id"/>

                    <filter name="my_requests" string='My Requests' domain="[('employee_id.user_id', '=', uid)]"/>
                    <filter name="to_be_approved" string='To Be Approved' domain="[('state', '=', 'wait_4_hr_mgr')]"/>

                    <group string="Group By">
                        <filter name="by_employee" string="Employee" context="{'group_by': 'employee_id'}"/>
                    </group>
                </search>
            </field>
        </record>

        <record model="ir.actions.act_window" id="action_final_exit">
            <field name="name">Final Exit</field>
            <field name="res_model">final.exit</field>
            <field name="view_mode">tree,form</field>
            <field name="context">{'search_default_my_requests': 1}</field>
            <field name="help" type="html">
                <p class="o_view_nocontent_smiling_face">Create a new Final Exit Request</p>
            </field>
        </record>


        <menuitem id="final_exit_menu" name="Final Exit" action="action_final_exit"
                  parent="alshrooq_employee_details.menu_hr_employee_self_service" sequence="1"/>

    </data>
</odoo>